# 蓝禾技能库 - 主入口技能

> 当用户发送「蓝禾技能：xxx」时，执行此技能。

## 技能信息

- **skill_id**: lanhe-skill-router
- **version**: 2.0
- **updated_at**: 2026-03-25

## 触发条件

用户消息以 `蓝禾技能：` 开头

## 执行流程

### 第 1 步：同步 GitHub 仓库

首先确保本地技能库与 GitHub 同步：

```bash
cd ~/.openclaw/workspace/lanhe-skills
git pull origin main
```

**注意**：如果本地没有 clone 过，先执行：
```bash
git clone https://github.com/Marerlee/lanhe-skills.git ~/.openclaw/workspace/lanhe-skills
```

---

### 第 2 步：解析用户问题

提取 `蓝禾技能：` 后面的内容作为搜索词：

```
用户: "蓝禾技能：标题怎么写"
→ 搜索词: "标题怎么写"
```

---

### 第 3 步：加载所有 SUMMARY.md

扫描 skills/ 目录下所有 SUMMARY.md 文件：

```bash
find ~/.openclaw/workspace/lanhe-skills/skills/ -name "SUMMARY.md" -type f
```

---

### 第 4 步：计算匹配度

对每个 SUMMARY.md 计算匹配得分：

```
匹配得分 = 
  (触发条件包含搜索词 ? 40分 : 0)
  + (语义关键词与搜索词重叠数 × 6分，最高 60分)
  + (skill_id 精确匹配 ? 20分 : 0)
```

取得分最高的技能作为最佳匹配。

---

### 第 5 步：返回结果

**匹配度 > 85%**：
直接执行最佳匹配的 SKILL.md

**匹配度 60-85%**：
显示 Top 2 技能，请用户确认：

```
🔍 找到相关技能，请确认：

1️⃣ 【技能名称】
   匹配度：XX%
   一句话描述
   「不是这个？」→ 回复「不是」

2️⃣ 【技能名称】
   匹配度：XX%
   一句话描述
```

**匹配度 < 60%**：
询问更具体的问题：

```
🤔 没有找到完全匹配的结果。请更具体地描述你的问题，例如：
• "亚马逊 listing 标题优化"
• "工厂设备异响怎么处理"
• "客户投诉退款怎么回复"
```

---

### 第 6 步：执行选中的技能

读取对应目录的 `SKILL.md`，按步骤执行。

---

### 第 7 步：执行后确认

```
✅ 已执行 【技能名称】

【输出内容...】

👆 满意吗？
• "满意" → 结束
• "不是这个" → 返回下一个最佳匹配（排除当前）
• "换一批" → 显示其他候选技能
```

---

## 纠错机制

- 用户说"不对"、"不是这个"、"换一批" → 返回下一个最佳匹配
- 连续 3 次匹配失败 → 引导用户细化问题

---

## 检索算法详解

### 评分权重

| 项目 | 最高分 | 说明 |
|------|--------|------|
| 触发条件匹配 | 40 | 完全包含搜索词得 40，部分匹配得 20 |
| 语义关键词重叠 | 60 | 每个重叠关键词得 6 分 |
| skill_id 精确匹配 | 20 | 用户直接提到 skill_id 时加分 |

### 匹配度计算

```
最终匹配度 = 得分 / 100 × 100%
```

| 匹配度 | 处理方式 |
|--------|---------|
| > 85% | 直接执行 |
| 60-85% | 显示 Top 2 确认 |
| < 60% | 要求细化问题 |

---

## 注意事项

- **每次执行前必须先 git pull**，确保技能库是最新的
- 只检索 SUMMARY.md，不直接读取 SKILL.md
- 检索路径：`~/.openclaw/workspace/lanhe-skills/skills/`
- 支持模糊匹配，不要求完全一致
- 匹配结果按得分降序排列
